import 'package:flutter/material.dart';

class Statisticks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Statisticks',
        style: TextStyle(
          color: Colors.blue,
        ),
      ),
    );
  }
}
